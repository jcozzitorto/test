class Lex {

public:
	ifstream infile;
	istream &in;

private:

	while (&in.get());
		{
			case IDENT:
				if (isalpha()) {
					TokenType = IDENT;
				}

			case ICONST:
				if (isdigit()) {
					TokenType = ICONST;
				}

			case SCONST:
				if(&in.get() == '"') {
					TokenType == SCONST;
				}


	}
